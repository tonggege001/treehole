<view class="mask" wx:if="{{show}}" bindtap="clickMask" catchtouchmove="stopmove">
  <view class="modal-content" style="height:{{height}}">
    <scroll-view scroll-y class="main-content">
      <slot></slot>
    </scroll-view>
  </view>
</view>